import 'package:flutter/material.dart';


Widget _buildlargeCard(BuildContext context, IconData icon, String title, String subtitle, ThemeData theme) {
  return GestureDetector(
    onTap: () {
      // Sin logica por ahora
    },
    
  );
}